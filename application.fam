# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="ktool",  # Must be unique
    name="KTool",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="ktool_app",
    stack_size=2 * 1024,
    fap_category="Tools",
    # Optional values
    # fap_version="0.1",
    fap_icon="ktool.png",  # 10x10 1-bit PNG
    # fap_description="A simple app",
    # fap_author="J. Doe",
    # fap_weburl="https://github.com/user/ktool",
    fap_icon_assets="images",  # Image assets to compile for this application
)
